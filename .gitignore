# OS
.DS_Store
Thumbs.db

# Editor swap files
*.swp

# Spec Kit local runtime state — regenerated by `specify extension add`/`remove`
# and not part of this repository's source (this repo is both a Spec Kit
# project and the rollout extension package source).
.specify/extensions/

# Test fixture directories (not part of the source tree, created and deleted during testing)
specs/999-*
